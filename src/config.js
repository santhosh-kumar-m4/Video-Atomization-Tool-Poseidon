// db config - need to setup postgres
// using pg library for connection

const { Pool } = require('pg');

// TODO: setup proper connection pool
// will add this once we have db credentials

module.exports = {
  // placeholder for now
};
